{
  "name": "@loom/codegen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'codegen build placeholder'",
    "clean": "rm -rf dist",
    "generate:flatbuffers": "tsx generate-flatbuffers.ts"
  }
}
